#include "utils.h"
#include <iostream>
using namespace std;

template <typename K,typename V>
void Utils::printNode(Node<K, V> *node) {
    int counter = 0;
    while ( node != null) {

    }
        cout << "$$$" << endl
}